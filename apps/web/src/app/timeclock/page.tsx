import { SurfacePlaceholder } from "@/components/shells/surface-placeholder";

export default function TimeclockPage() {
  return (
    <SurfacePlaceholder
      title="Timeclock"
      summary="Clock-in, clock-out, break tracking, and driver availability linkage are anchored here."
    />
  );
}
