import { SurfacePlaceholder } from "@/components/shells/surface-placeholder";

export default function RegisterPage() {
  return (
    <SurfacePlaceholder
      title="Register"
      summary="Register sessions, cash drawer actions, reconciliation, and reprint flows are separated into this route."
    />
  );
}
