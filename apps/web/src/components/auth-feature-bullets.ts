/**
 * Shared value props for the left column on login and signup.
 */
export const AUTH_FEATURE_BULLETS = [
  "Your usual order, ready instantly",
  "Get rewards on every order",
  "Secure, effortless login every time",
] as const;
